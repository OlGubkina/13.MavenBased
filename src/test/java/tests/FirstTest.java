package tests;

import org.junit.Assert;
import org.junit.Test;

public class FirstTest {
    @Test
    public void sumTest () {
        Assert.assertTrue("Message if condition = false", true);
    }
}
